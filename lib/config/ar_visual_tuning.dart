import 'package:flutter/material.dart';

class ArVisualTuning {
  static const Duration cameraRevealDelay = Duration(milliseconds: 180);
  static const Duration cameraFadeDuration = Duration(milliseconds: 420);
  static const Duration stylistDelay = Duration(seconds: 6);
  static const Duration initializationTimeout = Duration(seconds: 5);

  static const Color loadingOverlayColor = Colors.black87;
  static const Color fitCardBg = Color(0xE2191919);
  static const Color fitCardBorder = Color(0x33FFFFFF);
  static const Color fitAccent = Color(0xFFE2C283);

  static const Color detectPositiveBg = Color(0xFF21492A);
  static const Color detectNegativeBg = Color(0xFF4A3131);
}
