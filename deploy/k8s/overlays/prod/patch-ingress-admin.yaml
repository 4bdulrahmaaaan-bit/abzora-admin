apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: abianzo-admin-ingress
spec:
  tls:
    - hosts:
        - admin.abianzo.com
      secretName: prod-abianzo-admin-tls
  rules:
    - host: admin.abianzo.com
